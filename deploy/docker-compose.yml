services:
  relay:
    build:
      context: ..
      dockerfile: deploy/relay.Dockerfile
    environment:
      HOST: 0.0.0.0
      PORT: 9000
    ports:
      - "9000:9000"
    restart: unless-stopped
